# Event Sales Assistant - Landing Page

Landing page для fake door теста Event Sales Assistant с функционалом сбора email и админ-панелью для аналитики.

## Возможности

### Основной сайт
- ✅ Адаптивный лендинг с описанием продукта
- ✅ Сбор email подписчиков через форму
- ✅ Автоматическое отслеживание посещений (IP, User-Agent, Referer)
- ✅ Валидация email
- ✅ Уведомление пользователей об успешной подписке

### Админ-панель
- ✅ Защищенный вход (логин/пароль)
- ✅ Статистика в реальном времени:
  - Общее количество собранных email
  - Общее количество посещений
  - Количество посещений за сегодня
- ✅ Просмотр всех собранных email с датами регистрации
- ✅ Просмотр последних 100 посещений с деталями
- ✅ Экспорт данных в TXT файлы:
  - Список email с датами
  - Статистика посещений

## Технологии

- **Backend**: Node.js + Express
- **Database**: SQLite (для простоты и портативности)
- **Frontend**: Vanilla JavaScript, адаптивный HTML/CSS
- **Контейнеризация**: Docker + Docker Compose

## Структура проекта

```
event-assistant/
├── server.js              # Основной сервер
├── package.json           # Зависимости Node.js
├── Dockerfile            # Конфигурация Docker образа
├── docker-compose.yml    # Конфигурация для развертывания
├── public/               # Статические файлы основного сайта
│   └── index.html       # Главная страница
├── admin/               # Файлы админ-панели
│   ├── login.html      # Страница входа
│   └── dashboard.html  # Дашборд с статистикой
└── data/               # База данных (создается автоматически)
    └── database.db    # SQLite база данных
```

## Быстрый старт

### Предварительные требования
- Docker и Docker Compose установлены
- Portainer (опционально, для удобного управления)
- Nginx Proxy Manager (для проксирования на event-assistant.nmq.su)

### Локальный запуск для тестирования

```bash
# Клонируйте проект
cd /path/to/event-assistant

# Запустите через Docker Compose
docker-compose up -d

# Сайт будет доступен на http://localhost:3000
```

### Развертывание на сервере с Portainer

1. **Загрузите проект на сервер**:
   ```bash
   # Через git или напрямую скопируйте папку event-assistant
   ```

2. **В Portainer создайте новый Stack**:
   - Перейдите в Stacks → Add stack
   - Название: `event-assistant`
   - Вставьте содержимое `docker-compose.yml` ИЛИ используйте Git repository
   - Deploy the stack

3. **Настройте Nginx Proxy Manager**:
   - Создайте новый Proxy Host
   - Domain Names: `event-assistant.nmq.su`
   - Scheme: `http`
   - Forward Hostname/IP: `event-assistant` (имя контейнера) или IP сервера
   - Forward Port: `3000`
   - Включите:
     - ✅ Cache Assets
     - ✅ Block Common Exploits
     - ✅ Websockets Support (опционально)
   - SSL:
     - ✅ Force SSL
     - ✅ Request a new SSL Certificate (Let's Encrypt)
     - ✅ Agree to terms

## Доступ к админ-панели

### URL админ-панели
```
https://event-assistant.nmq.su/admin
```

### Учетные данные по умолчанию
⚠️ **ВАЖНО**: Смените пароль после первого входа!

- **Логин**: `admin`
- **Пароль**: `admin123`

### Смена пароля администратора

Чтобы сменить пароль, подключитесь к контейнеру и выполните:

```bash
# Войдите в контейнер
docker exec -it event-assistant sh

# Запустите Node.js интерактивную оболочку
node

# Выполните следующий код (замените 'новый_пароль' на ваш пароль):
const bcrypt = require('bcryptjs');
const Database = require('better-sqlite3');
const db = new Database('./data/database.db');
const newPassword = 'новый_пароль';
const hash = bcrypt.hashSync(newPassword, 10);
db.prepare('UPDATE admin_users SET password_hash = ? WHERE username = ?').run(hash, 'admin');
console.log('Password updated!');
process.exit();
```

## API Endpoints

### Публичные
- `GET /` - Главная страница
- `POST /api/subscribe` - Подписка на email рассылку

### Админские (требуют авторизации)
- `GET /admin` - Страница входа
- `POST /admin/login` - Авторизация
- `GET /admin/logout` - Выход
- `GET /admin/dashboard` - Дашборд
- `GET /admin/api/stats` - Статистика (JSON)
- `GET /admin/api/emails` - Список email (JSON)
- `GET /admin/api/visits` - Список посещений (JSON)
- `GET /admin/api/export/emails` - Экспорт email в TXT
- `GET /admin/api/export/visits` - Экспорт посещений в TXT

## База данных

Приложение использует SQLite для хранения данных. База создается автоматически при первом запуске.

### Таблицы:

**emails**
- `id` - AUTO INCREMENT PRIMARY KEY
- `email` - TEXT UNIQUE
- `created_at` - DATETIME (автоматически)

**visits**
- `id` - AUTO INCREMENT PRIMARY KEY
- `ip` - TEXT
- `user_agent` - TEXT
- `referer` - TEXT
- `visited_at` - DATETIME (автоматически)

**admin_users**
- `id` - AUTO INCREMENT PRIMARY KEY
- `username` - TEXT UNIQUE
- `password_hash` - TEXT (bcrypt хеш)

### Резервное копирование

База данных хранится в `/app/data/database.db` внутри контейнера и маппится на `./data/database.db` на хосте.

Для резервного копирования просто скопируйте файл `database.db`:

```bash
cp data/database.db data/database.backup.$(date +%Y%m%d).db
```

## Troubleshooting

### Проверка логов
```bash
# Через Docker
docker logs event-assistant

# Следить за логами в реальном времени
docker logs -f event-assistant
```

### Перезапуск контейнера
```bash
docker-compose restart
```

### Полная пересборка
```bash
docker-compose down
docker-compose up -d --build
```

### Проблемы с правами на database.db
```bash
# Дайте права на папку data
chmod 777 data
```

## Безопасность

⚠️ **Рекомендации**:

1. **Обязательно смените пароль администратора** после развертывания
2. Используйте **HTTPS** (настроено через Nginx Proxy Manager)
3. Регулярно делайте **резервные копии** базы данных
4. Ограничьте доступ к `/admin` через Nginx (опционально, whitelist IP)
5. Рассмотрите добавление **rate limiting** для защиты от спама

## Будущие улучшения (после основной работы)

- [ ] SEO оптимизация (meta tags, Open Graph, Schema.org)
- [ ] Google Analytics / Yandex Metrika интеграция
- [ ] Более детальная аналитика поведения пользователей
- [ ] A/B тестирование различных версий страницы
- [ ] Email уведомления при новой подписке
- [ ] Экспорт в CSV формат
- [ ] Двухфакторная аутентификация для админа

## Поддержка

Если возникли проблемы:
1. Проверьте логи контейнера
2. Убедитесь что порт 3000 не занят
3. Проверьте конфигурацию Nginx Proxy Manager
4. Убедитесь что DNS запись для event-assistant.nmq.su настроена правильно

---

**Готово к использованию для fake door теста! 🚀**
